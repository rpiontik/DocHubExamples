imports:
  - Datalake/root.yaml
  - Metamodel/root.yaml

docs:
  # Создаем стартовую страницу
  dochub.welcome:
    type: mkr-grid
    source:
      type: rows
      items:
        - src: "@document/Common.doc.ApplicationsListOfInformationSystems"
